import 'package:flutter/material.dart';

const kWidth0 = SizedBox();
const kWidth = SizedBox(width: 10);
const kHeight = SizedBox(height: 10);
const kHeight20 = SizedBox(height: 20);
const kHeight30 = SizedBox(height: 30);
const kHeight55 = SizedBox(height: 55);

// Padding
const kMovieListPadding = EdgeInsetsDirectional.symmetric(horizontal: 10);
const kPadding10 = EdgeInsets.all(10);

// Border
final kBorderRadius30 = BorderRadius.circular(30);
final kBorderRadius8 = BorderRadius.circular(8);
final kMButtonBorder =
    RoundedRectangleBorder(borderRadius: BorderRadius.circular(8));
